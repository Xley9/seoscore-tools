{
  "name": "seoscore-tools",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "npm run build && wrangler pages dev src/site --port 8788",
    "deploy": "npm run build && wrangler pages deploy src/site --project-name seoscore-tools",
    "preview": "npm run build && npx serve src/site -p 3000"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "wrangler": "^3.0.0"
  }
}
