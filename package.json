{
  "name": "seoscore-tools",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy && node scripts/indexnow-submit.js",
    "indexnow": "node scripts/indexnow-submit.js",
    "preview": "npm run build && npx serve src/site -p 3000"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "wrangler": "^3.0.0"
  }
}
